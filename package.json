{
  "name": "voice-ai-pipeline",
  "version": "1.0.0",
  "description": "Dialogue simulation pipeline for voice AI cashier",
  "type": "module",
  "scripts": {
    "inject": "node scripts/inject-env.js",
    "import-tm1": "node scripts/import-taskmaster.js",
    "pipeline": "node pipeline/run.js",
    "pipeline:single": "node pipeline/run.js --count 1",
    "pipeline:batch": "node pipeline/run.js --count 100"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "papaparse": "^5.4.1"
  }
}
